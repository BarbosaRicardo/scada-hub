{
  "buildCommand": "cd site && npm install && npm run build",
  "outputDirectory": "site/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "crons": [
    {
      "path": "/api/refresh-training",
      "schedule": "0 6 * * 1"
    }
  ]
}
