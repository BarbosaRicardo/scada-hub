# Humor Trim List — for review (nothing edited yet)

Candidate lines where the dry humor may tip past "a tad too much."
Quiz explanations appear AFTER answering, so they earn more rope than page copy.
Mark the ones to soften and I will batch-edit. Default recommendation: trim the
"punches at people" + "mental-health joke" rows, keep situational dark humor.

**12 candidates across 8 guides**


## modbus
- **[punches at people]** `data/quizzes.js` — “…readable hex), and TCP (over Ethernet). Anyone selling you "Modbus UDP-Lite" is lying or confused — probably both.'     },…”
- **[punches at people]** `data/quizzes.js` — “…Master always initiates. Slaves are completely passive — they sit there waiting like interns on their first day. A slave…”
- **[name-calling]** `data/quizzes.js` — “…very byte as two ASCII hex characters. You can literally read the frames with a dumb terminal. This is great for debugging and terrible for bandwidth — ev…”
- **[name-calling]** `data/quizzes.js` — “…segment','It must be protocol-aware to regenerate proper inter-frame timing — a dumb signal amplifier cannot correctly reconstruct frame boundaries','It m…”
- **[name-calling]** `data/quizzes.js` — “…input side, buffer the complete frame, and retransmit it with correct timing. A dumb electrical repeater passes degraded timing into the second segment. I…”
- **[name-calling]** `data/quizzes.js` — “…cond segment. If the first segment\'s inter-character gaps are slightly long, a dumb repeater passes those degraded timings on, causing framing errors on…”
- **[name-calling]** `pages/ASCII.jsx` — “…ASCII makes sense when: (1) you're debugging and want to read raw frames on a dumb terminal,         (2) your hardware has bad binary timing characteris…”
- **[name-calling]** `pages/ASCII.jsx` — “…acters. That overhead buys one thing RTU can't: you can read raw traffic on any dumb terminal or serial analyzer without a decoder. Connect a serial termi…”
- **[finger-wag]** `pages/Intro.jsx` — “…vice versa —         you'll get nothing but silence and confusion. Been there. Don't be that engineer.       </Callout>        <CheckYourself         q…”

## rtac
- **[melodrama]** `data/quizzes.js` — “…HMI pages, reports, and security settings. There is no other tool. Learn it or weep.',     },…”
- **[melodrama]** `data/quizzes.js` — “…. Set the scan rate too slow and events are missed. Set it too fast and the CPU cries.',       r…”

## ignition
- **[finger-wag]** `pages/Intro.jsx` — “…the Gateway machine         during testing) and fails completely in production. Don't be that engineer.       </Callout>        <AnalogyCard analogy={AN…”